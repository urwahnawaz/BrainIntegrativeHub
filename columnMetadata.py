class ColumnMetadata:
    def __init__(self, name, isDataset, isDatabase, isBrainDataset):
        self.name = name
        self.isDataset = isDataset
        self.isDatabase = isDatabase
        self.isBrainDataset = isBrainDataset